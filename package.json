{
  "name": "slms-root",
  "version": "1.0.0",
  "description": "Smart Library Management System - Unified Master Application",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd frontend-react && npm install && npm run build && cd ../admin-angular && npm install --legacy-peer-deps && npx --yes ng build --output-path ../frontend-react/dist/admin --base-href /admin/ && cd ../backend && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "test": "node run-all-tests.js",
    "test:backend": "cd backend && npm test",
    "test:react": "cd frontend-react && npm test",
    "test:angular": "cd admin-angular && npm test",
    "seed": "cd backend && npm run seed",
    "clean-db": "cd backend && npm run clean-db",
    "wipe-atlas": "cd backend && npm run wipe-atlas"
  }
}
